const Button = (props)=> {
   return (
   		<button>{props.label}</button>
   );
}


ReactDOM.render(<Button label="Fox"/>, mountNode);

class Button extends React.Component {

state={counter:0};
handleClick =()=> {
this.setState("prevState)=")
}
}
}
